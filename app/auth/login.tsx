import { SafeAreaView } from 'react-native';

import { Text } from '@/components/Themed';

const Login = () => {
  return (
    <SafeAreaView>
      <Text>Login</Text>
    </SafeAreaView>
  );
};

export default Login;
